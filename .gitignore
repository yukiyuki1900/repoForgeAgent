node_modules/
dist/
web/dist/
web/node_modules/

# 环境变量与密钥（勿提交）
.env
.env.*
!.env.example

# 分析产物（索引、checkpoint、报告）
.reposurgeon/

*.log
.DS_Store
.idea/
.vscode/

# TypeScript 增量编译缓存
*.tsbuildinfo

# 测试沙箱（e2e 用例会在里面建临时 git 仓库）。
# 正常跑完由 afterEach 清理，但测试被中断时会残留，所以必须忽略——
# 否则一个嵌套的 git 仓库会被顺手提交进来。
.tmp-apply-test/
.tmp-plan-test/
.tmp-jobs-test/
.tmp-prune-test/
.tmp-execute-test/
