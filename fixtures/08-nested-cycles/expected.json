{
  "title": "交织环合并为单个强连通分量",
  "status": "expected-pass",
  "why": "src/a → b → c → a 与 b → d → b 两个环共享节点 b，四个文件互相可达，应识别为一个包含 4 个文件的强连通分量而不是两个独立的环。这个用例保护 Tarjan 的迭代实现——递归版在万级文件的长依赖链上会栈溢出，改写成显式栈后必须验证分量合并逻辑仍然正确。",
  "expect": {
    "cycles": [["src/a.ts", "src/b.ts", "src/c.ts", "src/d.ts"]]
  }
}
